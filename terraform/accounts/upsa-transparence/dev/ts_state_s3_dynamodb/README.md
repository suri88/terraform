# UPSA - S3 Bucket creation

Configuration in this directory creates a s3 bucket.

This outputs the bucket name and region.

## Usage

To run this code you need to execute:

```bash
$ terraform init
$ terraform plan
$ terraform apply
```